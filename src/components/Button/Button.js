import React from "react";

const Button = () => {
  return (
    <div>
      <button type="submit">
        <span>+ </span>
        <span>New</span>
      </button>
    </div>
  );
};

export default Button;
